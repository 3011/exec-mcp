{
  "name": "exec-mcp",
  "version": "0.3.0",
  "description": "Bounded SSH remote execution and file-transfer gateway for trusted MCP clients",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node --check src/*.js && node --check scripts/*.js && node --check test/*.test.js",
    "validate": "bash scripts/validate.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/3011/exec-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/3011/exec-mcp/issues"
  },
  "homepage": "https://github.com/3011/exec-mcp#readme",
  "keywords": [
    "mcp",
    "ssh",
    "remote-execution",
    "gateway",
    "model-context-protocol"
  ]
}
